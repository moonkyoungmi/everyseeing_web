<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.es.web.login.LoginMapper">

	<!-- 회원 정보 가져오기 -->
	<select id="getMemberInfo" parameterType="HashMap">
		SELECT
			  idx AS idx_member
			, email
			, password
		FROM
			MEMBER
		WHERE
			email = #{email}
		AND status = 'A'
	</select>
	
	<!-- 로그인 기록 추가 -->
	<insert id="addLoginHistory" parameterType="HashMap">
		INSERT INTO LOGIN_HISTORY(
			  idx_member
			, ip
			, login_yn
		) VALUES (
			  #{idx_member}
			, #{ip}
			, #{login_yn}
		)	
	</insert>
</mapper>